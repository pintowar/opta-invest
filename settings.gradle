pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
}
rootProject.name = 'opta-invest'
include 'solver', 'web-client', 'web-server-boot', 'web-server-quarkus', 'web-server-micronaut'
